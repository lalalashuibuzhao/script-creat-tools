# UTF-8
#
# For more details about fixed file info 'ffi' see:
# http://msdn.microsoft.com/en-us/library/ms646997.aspx
VSVersionInfo(
  ffi=FixedFileInfo(
    # filevers and prodvers should be always a tuple with four items: (1, 2, 3, 4)
    # Set not needed items to zero 0.
    filevers=(0, 2, 0, 0),
    prodvers=(0, 2, 0, 0),
    # Contains a bitmask that specifies the valid bits 'flags'r
    mask=0xb,
    # Contains a bitmask that specifies the Boolean attributes of the file.
    flags=0x8,
    # The operating system for which this file was designed.
    # 0x4 - NT and there is no need to change it.
    OS=0x4,
    # The general type of file.
    # 0x1 - the file is an application.
    fileType=0x1,
    # The function of the file.
    # 0x0 - the function is not defined for this fileType
    subtype=0x0,
    # Creation date and time stamp.
    date=(0, 0)
    ),
  kids=[
    StringFileInfo(
      [
      StringTable(
        u'080904B0',
        [StringStruct(u'CompanyName', u'FuLe'),
        StringStruct(u'ProductName', u'Script Creat'),
        StringStruct(u'FileDescription', u'Script Creat'),
        StringStruct(u'InternalName', u'Script Creat Gui'),
        StringStruct(u'OriginalFilename', u'Script Creat'),
        StringStruct(u'FileVersion', u'0.2.0.0'),
        StringStruct(u'ProductVersion', u'Unidentified build'),
        StringStruct(u'LegalCopyright', u'Copyright  © 2020 '),
        StringStruct(u'PrivateBuild', u'Unidentified build')])
      ]), 
    VarFileInfo([VarStruct(u'Translation', [2057, 1200])])
  ]
)
